# fullstack-internal

React app with product search and category filter.

## Run

```bash
npm install
npm run dev
```

## Features

- Search products by name (case insensitive)
- Filter by category (All, Electronics, Clothing, Home)
- View product name, category, and price
